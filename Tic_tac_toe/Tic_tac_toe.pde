

void setup(){
  size(800,600);
}

void draw(){
  for(int x = 0; x < 3; x++) {
  for(int y = 0; y < 3; y++)
    
  
  rect(200*x,200*y,200,200);
  }
  }
  


void mousePressed(){
  println( mouseX, mouseY);
  if( mouseX > 0 && mouseX < 200){
  if( mouseY > 0 && mouseY < 200);
 
  
  
 println("noob");
  }
}
